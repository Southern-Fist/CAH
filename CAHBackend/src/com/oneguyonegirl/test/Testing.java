package com.oneguyonegirl.test;

public class Testing {

	//public static void main(final String[] args) {

			/*
			 * String text =
			 * "For my next trick, I will pull __________ out of __________.";
			 * String[] pieces = text.split("__________"); for (int i = 0; i <
			 * pieces.length; i++) System.out.println("Pieces - " + pieces[i]);
			 */

			/*System.out.println("Hash : " + generateHashSalt(2));

			PasswordContainer pcStretched = new PasswordContainer();
			pcStretched.setPassword("test");
			pcStretched = generateHash(pcStretched, STRETCHED_HASH_ALGO);

			System.out.println("Length : " + pcStretched.getSalt().length
					+ " String : " + hexify(pcStretched.getSalt()));

			PasswordContainer pcNormal = new PasswordContainer();
			pcNormal.setPassword("test");
			pcNormal = generateHash(pcNormal, NORMAL_HASH_ALGO);

			System.out.println("Clear Password: " + pcNormal.getPassword()
					+ " / Normal Hash: " + pcNormal.getHashedPassword()
					+ " / Salt: " + pcNormal.getSalt().toString() + " / length: "
					+ pcNormal.getHashedPassword().length());

			System.out.println("Clear Password: " + pcStretched.getPassword()
					+ " / Stretched Hash: " + pcStretched.getHashedPassword()
					+ " / Salt: " + pcStretched.getSalt().toString()
					+ " / length: " + pcStretched.getHashedPassword().length());

			System.out.println("is normal check "
					+ isPasswordValid(pcNormal, NORMAL_HASH_ALGO));
			System.out.println("is stretched check "
					+ isPasswordValid(pcStretched, STRETCHED_HASH_ALGO));
			*/
		//}
}
